replicaCount: 1

image:
  # GCP Artifact Registry. build-images.sh pushes both :latest and :<sha>.
  repository: us-central1-docker.pkg.dev/whisperops/whisperops-images/budget-controller
  pullPolicy: Always
  tag: "latest"

pollInterval: 60

langfuse:
  secretName: langfuse-credentials
  publicKeyEnvVar: LANGFUSE_PUBLIC_KEY
  secretKeyEnvVar: LANGFUSE_SECRET_KEY
  publicKeySecretKey: LANGFUSE_PUBLIC_KEY
  secretKeySecretKey: LANGFUSE_SECRET_KEY
  hostEnvVar: LANGFUSE_HOST
  host: "https://us.cloud.langfuse.com"

# Reference the per-namespace AR pull Secret (mirrored in by Reflector).
imagePullSecrets:
  - name: ar-pull-secret

resources:
  requests:
    cpu: "50m"
    memory: "64Mi"
  limits:
    cpu: "200m"
    memory: "128Mi"

serviceAccount:
  create: true
  name: budget-controller

securityContext:
  runAsNonRoot: true
  runAsUser: 1000
  readOnlyRootFilesystem: true
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL

podSecurityContext:
  fsGroup: 1000
  seccompProfile:
    type: RuntimeDefault

nodeSelector: {}
tolerations: []
affinity: {}
