apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-bootstrap-job
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  project: default
  source:
    repoURL: http://gitea.gitea.svc.cluster.local/whisperops/whisperops.git
    targetRevision: HEAD
    path: platform/helm/platform-bootstrap-job
    helm:
      releaseName: platform-bootstrap-job
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: whisperops-bootstrap
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
