apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: agent-prompts
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: default
  source:
    repoURL: http://my-gitea-http.gitea.svc.cluster.local:3000/whisperops/whisperops.git
    targetRevision: HEAD
    path: platform/helm/agent-prompts
    helm:
      releaseName: agent-prompts
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: whisperops-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
