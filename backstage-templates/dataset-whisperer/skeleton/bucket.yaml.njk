apiVersion: storage.gcp.upbound.io/v1beta1
kind: Bucket
metadata:
  name: agent-{{ values.agent_name }}-{{ values.suffix }}
  namespace: agent-{{ values.agent_name }}-{{ values.suffix }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  forProvider:
    location: {{ values.region | default('US-CENTRAL1') }}
    publicAccessPrevention: enforced
    versioning:
      - enabled: true
    labels:
      whisperops-agent: agent-{{ values.agent_name }}-{{ values.suffix }}
      dataset: {{ values.dataset_id }}
  providerConfigRef:
    name: default
  deletionPolicy: Delete
