apiVersion: cloudplatform.gcp.upbound.io/v1beta1
kind: ServiceAccount
metadata:
  name: agent-{{ values.agent_name }}
  namespace: agent-{{ values.agent_name }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  forProvider:
    displayName: "Dataset Whisperer Agent: {{ values.agent_name }}"
    description: "Service account for agent-{{ values.agent_name }}"
  providerConfigRef:
    name: default
