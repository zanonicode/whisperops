apiVersion: kyverno.io/v1
kind: Policy
metadata:
  name: agent-egress-policy
  namespace: agent-${{ values.agent_name }}
  annotations:
    argocd.argoproj.io/sync-wave: "2"
    policies.kyverno.io/title: Agent-specific Egress Allowlist
    policies.kyverno.io/description: >-
      Per-agent namespace egress policy for ${{ values.agent_name }}.
      Allows traffic to sandbox, Supabase, Anthropic API, and DNS only.
spec:
  validationFailureAction: Enforce
  rules:
    - name: restrict-egress
      match:
        any:
          - resources:
              kinds:
                - NetworkPolicy
      validate:
        message: "Egress NetworkPolicies in this namespace must be named 'default-egress'."
        pattern:
          metadata:
            name: default-egress
