# Age / SOPS private keys — defensive even though SOPS is not currently wired
age.key
*.age

# Datasets — bucket is source of truth post-upload (110 MB local copy)
datasets/*.csv
datasets/*.zip

# Claude Code project metadata (agents, KBs, SDD artifacts) — operator-local
.claude/

# Operator notes
notes/

# Terraform
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfplan
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
dist/
build/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / TypeScript
node_modules/
.next/
next-env.d.ts
out/
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Docker
*.tar

# Env files
.env
.env.*
!.env.example
